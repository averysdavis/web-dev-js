# Animations with jQuery
$(selector).animate({params},speed);
params: what is being changed

lowerCamelCase:"string"

speed: slow, fast, or ms

"string"

$("div).animate({
height:"300px",
width:"300px"},"slow");
animations are in a cue